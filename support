<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Поддержка — В Эфире</title>
  <style>
    :root {
      --bg: #0b0b10;
      --card: rgba(255,255,255,0.06);
      --text: rgba(255,255,255,0.92);
      --muted: rgba(255,255,255,0.70);
      --muted2: rgba(255,255,255,0.55);
      --line: rgba(255,255,255,0.12);
      --accent: rgba(168,85,247,0.95);
      --shadow: 0 10px 30px rgba(0,0,0,0.35);
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial;
      background: radial-gradient(1200px 700px at 20% 0%, rgba(168,85,247,0.22), transparent 55%),
                  radial-gradient(900px 600px at 90% 10%, rgba(255,77,77,0.16), transparent 55%),
                  var(--bg);
      color: var(--text);
      line-height: 1.55;
    }

    .wrap {
      max-width: 920px;
      margin: 0 auto;
      padding: 40px 18px 60px;
    }

    header { margin-bottom: 18px; }

    .badge {
      display: inline-block;
      padding: 6px 10px;
      border: 1px solid var(--line);
      background: rgba(255,255,255,0.04);
      border-radius: 999px;
      color: var(--muted);
      font-size: 13px;
    }

    h1 {
      margin: 12px 0 8px;
      font-size: 32px;
      letter-spacing: -0.02em;
    }

    .card {
      margin-top: 18px;
      background: var(--card);
      border: 1px solid var(--line);
      border-radius: 16px;
      padding: 20px 18px;
      box-shadow: var(--shadow);
      backdrop-filter: blur(10px);
    }

    .divider {
      height: 1px;
      background: var(--line);
      margin: 18px 0;
    }

    h2 {
      font-size: 20px;
      margin: 0 0 10px;
    }

    p { margin: 0 0 10px; }

    a {
      color: rgba(255,255,255,0.92);
      text-decoration: none;
      border-bottom: 1px dashed rgba(255,255,255,0.35);
    }

    a:hover { border-bottom-color: var(--accent); }

    footer {
      margin-top: 18px;
      color: var(--muted2);
      font-size: 13px;
    }

    @media (max-width: 520px) {
      h1 { font-size: 26px; }
      .wrap { padding-top: 28px; }
      .card { padding: 16px 14px; }
    }
  </style>
</head>

<body>
  <div class="wrap">
    <header>
      <span class="badge">В Эфире • Поддержка</span>
      <h1>Служба поддержки</h1>
    </header>

    <div class="card">

      <h2>Связаться с нами</h2>
      <p>
        Если у вас возникли вопросы, проблемы или предложения по улучшению приложения,
        пожалуйста, напишите нам на email:
      </p>

      <p>
        <strong>Email:</strong>
        <a href="mailto:developtonyt@icloud.com">developtonyt@icloud.com</a>
      </p>

      <div class="divider"></div>

      <h2>О приложении</h2>
      <p>
        «В Эфире» — это развлекательное приложение-симулятор,
        создающее эффект прямого эфира с виртуальными зрителями.
      </p>

      <div class="divider"></div>

      <h2>Время ответа</h2>
      <p>
        Мы стараемся отвечать на обращения в течение 24–48 часов.
      </p>

      <footer>
        © 2026 В Эфире
      </footer>
    </div>
  </div>
</body>
</html>
